#include <stdio.h>
#include <stdlib.h>

int main() {
   /*unsigned char x;
    for(x=0; x<255; x++)
        printf("%d -> %c\n", x, x);*/

    char y;
    for(y=-128; y<127; y++)
        printf("%d -> %c\n", y, y);
    
        
}